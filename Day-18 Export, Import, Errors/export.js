// Named Export and Import vs Default one
// - **Named export**: Export multiple named things, import using `{ }`.
// - **Default export**: Export one main thing, import without `{ }`, and name freely.
// note - ./ is for same level while ../ is for one level up. ../../ is two level up.
const age = 25;
const name = "John Doe";
